# k8s/monitoring/grafana-dashboards-cm.yaml
# Grafana sidecar auto-discovers ConfigMaps labeled grafana_dashboard: "1"
apiVersion: v1
kind: ConfigMap
metadata:
  name: mukti-grafana-dashboards
  namespace: mukti-monitoring
  labels:
    grafana_dashboard: "1"
data:
  mukti-overview.json: |
    # Paste contents of dashboards/mukti-overview.json here
  mukti-agents-llm.json: |
    # Paste contents of dashboards/mukti-agents-llm.json here
