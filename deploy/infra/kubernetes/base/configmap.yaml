apiVersion: v1
kind: ConfigMap
metadata:
  name: nexus-config
data:
  APP_ENV: "staging"
  LOG_LEVEL: "INFO"
  MAX_CONCURRENT_WORKERS: "5"
  TOKEN_BUDGET_PER_TASK: "8000"
  NEXT_PUBLIC_API_URL: "http://nexus-backend:8000"
