# Runtime dependencies. Kept in sync with [project].dependencies in
# pyproject.toml, which is the source of truth for `pip install -e .`.
# This file exists for `pip install -r requirements.txt` workflows and for
# the CI job, which installs these names explicitly.
click>=8.0.0
rich>=13.0.0
pydantic>=2.0.0
requests>=2.31.0
psutil>=5.9.0
PyYAML>=6.0.0

# Build backend; not a [project].dependencies entry because pyproject
# declares it under [build-system].requires instead.
setuptools>=61
